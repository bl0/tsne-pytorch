# pytorch based TSNE implementation 

## Requirements

python3, pytorch, numpy, scipy, matploblib, sklearn


## Run

Just run `jupyter notebook --ip 0.0.0.0` in terminal and use browser to open the notebook.
